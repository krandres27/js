const numberValue = 0

// or operator validates if left item is falsy
const orOperator = numberValue || 1
orOperator

// while nullish operator, checks if left item is null or undefined
const nullishOperator = numberValue ?? 1
nullishOperator

let undefinedVar
const nullishOperator2 = undefinedVar ?? 1
nullishOperator2